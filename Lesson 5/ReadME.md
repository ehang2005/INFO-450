# Soccer Match Data Fetcher

A lightweight Python script that retrieves and parses historical soccer match data from a remote JSON endpoint using the `requests` library.

## Prerequisites

Make sure you have Python installed along with the `requests` package:

```bash
pip install requests
